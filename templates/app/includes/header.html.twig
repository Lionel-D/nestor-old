{% extends 'global/includes/header.html.twig' %}

{% block main_nav %}{% endblock %}

{% block header_buttons %}
    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('admin_dashboard') }}" class="btn btn-sm btn-outline-secondary mr-2">
            <i class="fas fa-tools"></i> {{ 'Admin panel'|trans({},'app') }}
        </a>
    {% endif %}
{% endblock %}